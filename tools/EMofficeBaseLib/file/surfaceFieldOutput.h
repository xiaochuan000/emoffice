#ifndef SURF_FIELD_H
#inlcude <complex>
template<class T> int exportField<T>(char* fname,T* field,bool so=false,bool binary=false);


template<class T> int exportField<T>(char* fname, T* field, bool so,bool binary)
{
return 0;
}
#define SURF_FIELD_H
